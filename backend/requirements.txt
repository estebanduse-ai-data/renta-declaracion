# ── Web framework ────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ── Base de datos ────────────────────────────────────────────────────
sqlalchemy==2.0.35
psycopg[binary]==3.2.2
alembic==1.13.3

# ── Validación y settings ────────────────────────────────────────────
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# ── Autenticación ────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1          # pinear < 4.1 — incompatibilidad con passlib 1.7.4

# ── Procesamiento de archivos Excel (carga masiva de declarantes) ────
openpyxl==3.1.5

# ── Multipart / carga de archivos desde el frontend ─────────────────
# Requerido por FastAPI para recibir UploadFile (multipart/form-data).
# Sin esta librería el endpoint POST /admin/importar-declarantes
# responde 422 Unprocessable Entity aunque el archivo se envíe correctamente.
python-multipart==0.0.9

# ── Testing ──────────────────────────────────────────────────────────
pytest==8.3.3
httpx==0.27.2

# ── Script CLI de importación (solo para scripts/, no para la API) ───
# requests>=2.31  # descomentar si se usa scripts/importar_declarantes.py
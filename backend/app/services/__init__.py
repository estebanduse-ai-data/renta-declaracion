# app/services — capa de servicios de negocio
#
# Módulos disponibles:
#   auditoria_service    — registrar_auditoria() (existía antes de Act. 3.3)
#   parametros_service   — obtener_parametros_vigentes() (existía antes de Act. 3.3)
#   declarante_service   — CRUD de declarantes, periodos e importación masiva (Act. 3.3)
#   liquidacion_service  — calcular_y_persistir() (Act. 3.3)